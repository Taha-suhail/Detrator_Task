import { StyleSheet, Text, View } from "react-native";
import React from "react";

const randomScren = () => {
  return (
    <View>
      <Text>randomScren</Text>
    </View>
  );
};

export default randomScren;

const styles = StyleSheet.create({});
